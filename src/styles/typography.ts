import { Typography } from "react-native-ui-lib";

Typography.loadTypographies({
  h2: {
    fontSize: 20,
    fontWeight: "600",
    lineHeight: 28,
  },
  sub: {
    fontSize: 16,
    lineHeight: 24,
  },
  p: {
    fontSize: 14,
    lineHeight: 22,
  }
});
